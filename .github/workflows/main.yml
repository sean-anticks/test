on: push
name: Test retire
jobs:
  build:
    name: Test retire
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-node@v1
      - add_ssh_keys:
        fingerprints: ${{ secrets.BACKEND }}
